# Rust build artifacts
/target
# NOTE: web/pkg/ (wasm-pack output) is committed on purpose so Cloudflare Pages can
# build with vite only (no Rust toolchain in CI). Rebuild it with `npm run build:wasm`.

# Node
web/node_modules/
node_modules/

# Secrets & local config
.env
.env.*
*.local

# Editor / tooling local state
.claude/
.DS_Store

# Build output
web/dist/
dist/
